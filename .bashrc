alias cm="echo 'lint running..' && bundle exec rake lint && echo 'validate running' && bundle exec rake validate && echo 'documentation running' && bundle exec rake spec SPEC_OPTS='--format documentation'"
